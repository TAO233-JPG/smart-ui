export type SSelectChange = (
  // eslint-disable-next-line no-unused-vars
  newVal: string | number,
  // eslint-disable-next-line no-unused-vars
  oldVal: string | number | undefined
) => void;

export type { SSelectProps } from "./props";
